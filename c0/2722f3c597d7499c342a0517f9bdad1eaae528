Added basic Makefile for installation.

The Makefile provides:
- Install target: copies all git-*.sh scripts to ~/bin without .sh extension
- Uses sed to transform script names (e.g., git-arch.sh -> git-arch)
- Uses xargs for bulk copying

Makes installation easier: just run 'make install' instead of manual copy.

/Claude-haiku@4.5
