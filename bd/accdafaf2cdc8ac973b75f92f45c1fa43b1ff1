Added git-delete-both.sh: Delete branch locally and remotely.

Deletes a branch from both local and remote repositories.

Usage: git delete-both [branch-name]
- Defaults to current branch if not specified
- Detaches HEAD if deleting current branch
- Deletes remote tracking branch first, then local

/Claude-haiku@4.5
