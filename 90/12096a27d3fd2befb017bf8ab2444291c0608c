Applied bash variable naming convention: lowercase for local variables.

Changed all local variable names in git-delorean.sh to lowercase to follow
bash conventions:
- Environment variables (PAGER, EDITOR, etc.) should be uppercase
- Shell internal variables (SHELL, BASH_VERSION, etc.) should be uppercase
- Local variables should be lowercase

This prevents accidental conflicts with environment/internal variables and
makes code intent clearer.

/Claude-haiku@4.5
